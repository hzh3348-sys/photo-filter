# 更新日志

## v5.1 (2026-07-23)

### 🐛 严重 Bug 修复

| 问题 | 说明 |
|------|------|
| **肤色检测单位错配（P0）** | OpenCV 8 位 LAB 值直接与 L\*a\*b\* 单位常量比较（A≈128 vs 上限 50），导致**自然肤色几乎全被判不合格**。v5.1 统一转为标准 L\*a\*b\* 单位后再判定，深/浅肤色均正常通过 |
| **重复检测开关不持久化** | `检测重复照片` 开关每次启动都重置为关闭；现纳入 QSettings 持久化 |
| **"并行线程数"设置不生效** | 创建 ProcessWorker 未传 max_workers，恒为 2；现真正生效 |
| **重复检测完成后表格插入空行** | 重复检测进度事件带空文件名，GUI 无条件插行；现跳过非照片事件 |
| **停止后仍弹"魏老师点评"** | 取消后仍发 finished_signal + 主线程 wait(3000) 卡界面；v5.1 新增 cancelled_signal，停止不再误报完成、不阻塞 UI |
| **欢迎窗每次启动都弹** | 只有勾选"下次不再显示"才写入 first_run；现点击开始即标记已引导 |

### 🗂️ 重复检测完善

| 改进 | 说明 |
|------|------|
| **RAW 优先真正实现** | `重复时优先保留 RAW` 此前是死设置：组内有 RAW 时保留 RAW，其余（含原代表）标记为重复 |
| **新增"重复敏感度"滑块** | 汉明距离阈值 1~15 可调（原写死 5），持久化 |
| **并行读图 + 整数汉明距离** | ThreadPoolExecutor 并行解码 + `(a^b).bit_count()`，比逐字符字符串比较快一个数量级 |

### ⚡ 性能优化

| 优化 | 说明 |
|------|------|
| **肤色 LAB 只转换一次** | 多人脸合照原来每张脸都对全图做 BGR→LAB（30 人脸 = 30 次全图转换）；现预计算一次全部复用 |
| **默认线程数 2 → 4** | 多核机器批量处理提速明显（设置中 1~8 可调） |
| **异步缩略图预览** | 大图/RAW 解码从主线程移到后台线程池，不再卡 UI；RAW 走内嵌 JPEG 快路径（原 QPixmap 直解 RAW 静默失败） |

### 🎯 检测算法改进

| 改进 | 说明 |
|------|------|
| **红眼瞳孔聚焦 + 连通域过滤** | 以虹膜关键点为中心取瞳孔 ROI（不再含眼睑），红色像素须成 ≥3 像素斑块；红眼皮/红眼影/零散噪点不误报 |
| **地平线单线检测** | 单条贯穿画面的倾斜地平线（海平面）现在也能检出（原要求 ≥3 条线成簇，漏检清晰单一地平线） |
| **表情得分有区分度** | 去掉 `+0.5` 归一化魔法数，得分随笑容线性上升（阈值处 0.5、2 倍阈值处满分），不再恒为 1.0 |
| **DetectionConfig 默认值统一** | 曝光 0.05/0.15 过时残留 → 统一引用 constants 0.50/0.50（v5.0 只拦极端曝光哲学）；表情阈值 0.3 → 0.25 |

### 🔧 其他修复

- 构图"通用检测"严格度滑块量程修正：显示 9.0° 实际用 4.5°（步长换算错误）
- 配置布尔解析安全化：`bool("false")==True` 陷阱（ini 配置下）用 `_as_bool` 统一处理
- `config.py` 重复定义的 blur_threshold 属性清理
- `PhotoResult` 新增 `note` 字段："未检测到人脸 / 人脸检测已关闭"不再作为失败原因展示（照片仍正常通过）
- `QThread.terminate` 仅在优雅停止超时后作为最后手段使用
- 人脸检测关闭时强制关闭表情/红眼子项（恢复设置不再出现"开关显示 ON 但实际不生效"）
- 移动（move）模式输出后，表格预览/定位更新到新路径（不再"文件不存在"）
- 停止后再开始分析时断开旧 worker 信号，消除结果竞态
- 设置对话框"恢复默认"补上"重复时优先保留 RAW"选项重置

### 🧪 测试

- 单元测试 29 → **99 个**：新增表情、红眼（瞳孔 ROI/噪点过滤）、肤色（单位修复回归）、构图（单线地平线）、模型（all_pass/fail_reason/默认值）、配置（布尔解析）、重复（并行路径/分组）等测试

---

## v5.0 (2026-07-23)

### 🚀 新增功能

| 功能 | 说明 |
|------|------|
| **合照模式** | 最优人脸 / 所有人脸 双模式切换，会议合照每张脸都需过关 |
| **笑容/表情检测** | 利用 MediaPipe 52 种 Blendshapes，检测笑容度、张嘴、表情僵硬 |
| **红眼检测** | HSV 瞳孔区域分析，识别闪光灯红眼，可调阈值 |
| **单击预览** | 结果列表单击右侧即时显示照片缩略图 |

### 🔧 改进

| 改进 | 说明 |
|------|------|
| **曝光与人脸解耦** | 曝光检测完全独立于人脸，极简像素统计，只拦极端情况 |
| **默认阈值大幅放宽** | 欠曝 50%、过曝 50%，仅拦截真正不可用的照片 |
| **肤色检测升级** | 全脸区域采样替代逐点采样，抗关键点定位误差 |
| **曝光检测极简化** | 移除中心加权/人脸加权等不稳定逻辑 |
| **界面 3 列重构** | 新增表情/红眼/合照模式控件 |

### 🐛 修复

- 打包版本 numpy 版本不一致导致运行时错误，改回系统包打包

---

## v4.0 (2026-06-15)

### 🚀 新增功能

| 功能 | 说明 |
|------|------|
| **RAW 格式支持** | CR2/NEF/ARW/DNG 等 21 种格式，三层回退 |
| **苹果风格拨动开关** | 平滑动画 |
| **欢迎引导** | 首次启动弹出欢迎对话框 |
| **人脸检测增强** | 三轮放大 + 4800px + 30 张脸 |

---

## v3.0 (2026-06-12)

### 🚀 新增功能

| 功能 | 说明 |
|------|------|
| **模糊检测** | 多区域最清晰判断法 + 宽容度滑块（10~200），浅景深人像不误判 |
| **重复照片检测** | dHash 感知哈希 + 汉明距离，自动标记相似/重复照片 |
| **人脸清晰度评分** | 针对人脸区域评估锐度 + 对比度 |
| **合照多人脸优选** | 检测多人脸时取最佳人脸（睁眼+肤色+清晰度最高）评估照片 |
| **深色模式** | 支持浅色/深色/跟随系统三种主题，设置中切换 |
| **人脸检测开关** | 关闭后仅检测曝光+构图+模糊，跳过 MediaPipe 大幅提速 |
| **地平线检测法** | 全新构图检测算法，只找长水平线判断倾斜，复杂场景（建筑/树林）不误判 |
| **设置持久化** | 自动记忆所有阈值、输入输出目录、窗口大小位置 |
| **拖拽导入** | 直接拖拽文件夹到窗口，自动填充路径 |
| **双击预览** | 双击结果行用系统默认程序打开原图 |
| **退出确认** | 分析进行中关闭窗口弹出确认，防止误关 |

### ⚡ 性能优化

| 优化 | 说明 |
|------|------|
| **多线程并行** | ThreadPoolExecutor（2-4 workers），大文件夹处理速度提升 2-3 倍 |
| **人脸检测提速** | 关闭人脸开关可跳过 MediaPipe 加载（省 3.6MB 内存 + 数秒启动时间） |
| **小脸检测优化** | 降低置信度阈值（0.4→0.25）+ 两轮放大检测，小幅人脸也能检出 |

### 🔧 检测算法改进

| 改进 | v2.0 | v3.0 |
|------|------|------|
| 肤色检测 | LAB 范围较窄，深色皮肤误判率高 | 大幅放宽（L:25→15, A:3→0, B:3→0），仅拒绝极端异常 |
| 模糊检测 | 无 | 多区域 4×4 网格 + 取前 25% 最清晰区域均值 |
| 构图检测 | 通用检测（MAD 角度一致性） | 地平线检测（推荐，减少误判）+ 通用检测（备选） |
| 构图严格度 | 固定 9° | 地平线 2°~12° / 通用 4°~20° 滑块可调 |

### 🏗️ 架构重构

```
v2.0: photo_filter_gui.py (单文件 ~700行)

v3.0: main.py                    入口
      ├── core/                  检测算法层（零 Qt 依赖）
      │   ├── pipeline.py        编排器 + MediaPipe 管理
      │   ├── exposure.py        曝光检测
      │   ├── skin_tone.py       肤色检测
      │   ├── eyes.py            睁眼检测 (EAR)
      │   ├── level.py           构图水平（地平线/通用）
      │   ├── blur.py            模糊检测 [新]
      │   ├── clarity.py         人脸清晰度 [新]
      │   ├── duplicate.py       重复检测 (dHash) [新]
      │   └── models.py          数据模型
      ├── gui/                   界面层
      │   ├── main_window.py     主窗口
      │   ├── worker.py          多线程处理
      │   └── theme_manager.py   主题管理 [新]
      ├── utils/                 工具层
      │   ├── constants.py       默认阈值
      │   ├── config.py          QSettings 持久化
      │   └── image_io.py        图片 I/O
      └── tests/                 29 个单元测试 [新]
```

### 🐛 Bug 修复

- 彩蛋（魏老师点评）触发条件：从"开启构图检测"改为"检测到人脸"，不再需要手动勾选构图
- 中文路径：MediaPipe 模型 `model_asset_buffer` 内存加载，打包后不再闪退
- 打包体积：Anaconda MKL 排除，安装包 <120MB

### 📦 打包

- Windows：`照片筛选GUI_v3.0_by_HZH.zip` (~118MB)
- macOS：`照片筛选工具_macOS.zip` (GitHub Actions 自动构建)
- 本地打包：`python build_v2.py`
- 开发测试：双击 `测试GUI.bat`

---

## v2.0 (2025-06)

- 初始 GUI 版本
- 曝光检测（直方图分析）
- 肤色检测（MediaPipe 468 关键点 + LAB 色彩空间）
- 睁眼检测（EAR 算法）
- 构图水平检测（Canny + 霍夫变换 + MAD）
- PySide6 图形界面
- Windows / macOS 双平台支持
- GitHub Actions 自动构建发布
